// hashing is used for pre storing and fetching.

#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }

    //precomputing
    int hash[13];
    for(int i=0;i<n;i++){
        
    }

    return 0;
}