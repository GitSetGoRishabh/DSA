#include<bits/stdc++.h>
using namespace std;

int main(){
    int a=5;
    int b=6;

    cout<<(a&b); // and operator
    cout<<(a|b); // or operator
    cout<<(a^b); // xor operator
    cout<<~(a); // not operator

    cout<<(a>>2); //right shift
    cout<<(a<<2); //left shift
    

}